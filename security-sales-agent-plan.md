# 공공기관 보안 영업 코파일럿 에이전트 — 프로젝트 초안

> 공공기관 입찰 공고를 모니터링해서 보안 관련 건을 골라내고,
> 우리 회사 보안 제품과 매칭해 영업 포인트까지 만들어주는 AI 에이전트.
> 클로드 코드와 함께 Phase 단위로 진행하기 위한 기획서.

---

## 1. 한 줄 소개

나라장터(조달청) 입찰 공고를 자동 수집 → 보안 관련 공고만 분류 → 우리 제품과 매칭 → 영업 토킹포인트 초안까지 만들어주는 AI 에이전트.

## 2. 목표 범위

- **MVP 목표**: "이번 주 보안 관련 공고 브리핑해줘"라고 하면, 에이전트가 알아서 공고를 찾아 분류하고 우리 제품과 매칭한 결과를 표로 보여준다.
- **포트폴리오 어필 포인트**: 보안 도메인 이해 + AI 에이전트 직접 구현 + 공공 조달 워크플로 정조준. 영업 직무에서 "우리 팀이 바로 쓸 만한 걸 만들었다"는 서사.
- **의도적으로 뺄 것 (1차)**: 자동 제안서 완성, 실제 입찰 자동 참여, 회원/권한 관리. → 보조 도구로만.

## 3. 데이터 소스

- **조달청_나라장터 입찰공고정보서비스** (공공데이터포털, 데이터셋 15129394)
  - URL: https://www.data.go.kr/data/15129394/openapi.do
  - 제공: 물품 / 용역 / 공사 / 외자 입찰공고 목록·상세, 기초금액, 참가가능지역 등
  - 검색조건: 날짜, 기관명, 품목명 등
  - ⚠️ 오퍼레이션이 **업무구분별(물품/용역/공사/외자)로 분리**돼 있음. 보안 솔루션은 주로 **용역(보안관제·정보보호 서비스)**, **물품(보안 장비)** 쪽. 해당 업무구분 오퍼레이션을 정확히 호출해야 정상 응답.
  - 사용 전 공공데이터포털에서 **활용신청 후 인증키 발급** 필요.

## 4. 기술 스택 (추천안)

| 영역 | 선택 | 이유 |
|------|------|------|
| 개발 도구 | Claude Code | 단계별로 같이 구현 |
| 언어 | Python | AI 에이전트·데이터 처리 생태계가 풍부 |
| AI | Anthropic API (Claude) + tool use | "도구를 호출하는 에이전트" 패턴의 핵심 |
| 데이터 | 나라장터 입찰공고 API | 위 3번 |
| UI | Streamlit | 프론트엔드 부담 없이 데모 가능한 대시보드 |
| 저장 | 1차: CSV/메모리 → 2차: SQLite | MVP는 가볍게 |

## 5. 에이전트 동작 흐름

사용자가 "이번 주 보안 공고 정리해줘"라고 하면:

1. 에이전트(Claude)가 **`search_bids` 도구**를 호출해 최근 공고 목록을 가져온다.
2. 각 공고가 보안 관련인지 판단하고 카테고리로 분류한다. (방화벽, 망분리, 접근통제(IAM), 보안관제(SOC), 백신/EDR, 정보보호 컨설팅 등)
3. 우리 회사 **제품 카탈로그**와 대조해 어떤 제품이 맞는지 + 그 근거를 정리한다.
4. 공고별 **영업 토킹포인트 초안**을 만든다. ("이 공고는 ○○ 요구 → 우리 △△ 제품이 □□로 충족")
5. 결과를 표/브리핑 형태로 출력한다.

> 여기서 "에이전트"란 자율 로봇이 아니라, **Claude가 필요한 도구(공고 검색 등)를 스스로 호출하며 여러 단계를 처리하는 워크플로**를 뜻함.

## 6. 단계별 작업 순서 (클로드 코드에 이 순서로 요청)

### Phase 0 — 셋업
- Python 가상환경 + Anthropic SDK 설치
- Anthropic API 키, 공공데이터포털 인증키를 `.env`로 관리
- Claude에 "안녕" 호출해서 응답 오는지 확인

### Phase 1 — 데이터 연결 (AI 없음)
- 나라장터 입찰공고 API(용역/물품)를 호출해 최근 공고 목록을 받아 출력
- 공고번호, 공고명, 발주기관, 마감일 정도만 깔끔히 파싱
- ✅ 검증: 실제 최근 공고가 콘솔에 뜨는지

### Phase 2 — 보안 관련 분류
- 가져온 공고 제목/내용을 Claude에게 보내 "보안 관련인지 + 어떤 카테고리인지" 판단
- 결과를 `{공고, 보안여부, 카테고리, 판단근거}` 형태로 정리
- ✅ 검증: 보안 공고와 무관한 공고가 잘 갈리는지

### Phase 3 — 제품 매칭
- 우리 회사 제품 카탈로그(아래 7번 형식)를 JSON으로 준비
- Claude가 공고 ↔ 제품을 매칭하고 근거를 설명
- 출력: 공고별 추천 제품 + 매칭 이유

### Phase 4 — 에이전트화 (tool use)
- `search_bids(업무구분, 키워드, 날짜범위)`를 **도구(function)**로 정의
- Claude가 사용자 요청을 받으면 스스로 이 도구를 호출 → 결과를 분류·매칭까지 한 번에
- ✅ 검증: "이번 달 정보보호 관련 용역 공고 찾아서 매칭해줘" 한 문장으로 동작하는지

### Phase 5 — 영업 산출물 생성
- 매칭된 공고별로 토킹포인트 / 제안 개요 초안 생성
- "고객 요구 → 우리 강점 → 차별점" 구조로

### Phase 6 — UI & 데모
- Streamlit 대시보드: 기간·카테고리 필터, 매칭 결과 표, 토킹포인트 보기, CSV 다운로드
- 데모용 화면 캡처/영상 확보

### Phase 7 — 확장 (선택)
- RFP/제안요청서 PDF 업로드 → 핵심 요구사항 추출 → 우리 제품 대응표 자동 생성
- 매일 아침 신규 보안 공고 요약 알림

## 7. 제품 카탈로그 형식 예시

```json
[
  {
    "제품명": "예시 차세대 방화벽",
    "카테고리": "네트워크 보안",
    "핵심기능": ["침입탐지", "애플리케이션 제어", "VPN"],
    "적합한_요구": ["방화벽 교체", "경계 보안 강화", "망분리"],
    "차별점": "공공기관 보안적합성 검증(CC인증) 보유"
  }
]
```

> 실제 제품 정보는 회사 공개 자료 범위 내에서만 사용. 비공개·민감 정보는 넣지 말 것.

## 8. 포트폴리오 정리용 메모

- "보안 도메인 + AI 에이전트 + 공공 조달"이라는 회사 사업과 정확히 겹치는 주제를 직접 구현했다.
- "Claude가 스스로 도구를 호출하는 에이전트 패턴(tool use)"을 다뤄봤다.
- AI 결과는 **사람이 최종 검수하는 보조 도구**로 설계 → 환각 리스크를 인지하고 통제했다는 점이 오히려 신뢰감을 줌.
- **보너스 한 줄**: 보안 영업이라면 "AI 에이전트 자체의 보안(프롬프트 인젝션, 권한 최소화 등)도 고려해 설계했다"고 덧붙이면 도메인 깊이가 돋보임.

## 9. 시작할 때 클로드 코드에 던질 첫 프롬프트 예시

```
공공기관 보안 영업을 돕는 AI 에이전트를 만들 거야. 첨부한 기획서의 Phase 0부터 시작하자.
Python 가상환경과 Anthropic SDK를 셋업하고, API 키는 .env로 관리해줘.
Claude에 간단한 메시지를 보내 응답이 오는지 확인하는 코드까지 만들어줘.
```

## 10. 주의사항

- API 키·인증키는 반드시 `.env`로 관리하고 깃에 커밋하지 말 것 (`.gitignore` 추가).
- 입찰 판단·참여 결정은 항상 사람이 최종 검증. 이 도구는 "후보를 빠르게 추려주는 보조 수단".
- 공공데이터·회사 제품정보 모두 공개 범위 내에서만 사용.

---

*이 문서는 출발점입니다. 진행하면서 범위를 늘리거나 줄여도 됩니다.*
